apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-list-config
data:
  # Database 설정
  MYSQL_DATABASE: "tododb"
  
  # Backend 설정 (필요시)
  CORS_ALLOWED_ORIGINS: "http://localhost:3000"
  
  # 기타 일반 설정값들
  LOG_LEVEL: "INFO"